# spring-boot-jsp-crud-postgresql
A straightforward name indicating that the repository contains a Spring Boot application using JSP for the frontend and PostgreSQL for the database, with CRUD operations.
